/**
 * Shared UI class strings — the exact same Tailwind recipes previously
 * hand-typed in every form (booking form, client login, admin editors).
 *
 * Cream theme: public site (booking form + client magic-link login).
 * Dark theme: admin panel (zinc/brass on the admin accent).
 */
export const inputCls =
  "w-full rounded-xl border border-ink/15 bg-white/80 px-4 py-3 text-sm text-ink outline-none transition placeholder:text-ink/40 focus:border-brass";

export const labelCls = "mb-2 block text-sm font-medium text-ink/70";

export const adminInputCls =
  "w-full rounded-xl border border-white/10 bg-zinc-950/60 px-3 py-2 text-sm outline-none focus:border-accent/60";